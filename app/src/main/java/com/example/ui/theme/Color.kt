package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// MAFQOOD Official Color Tokens (Spec 3.1)
val Navy900 = Color(0xFF12213B) // Primary text; dark hero surfaces (splash, AI, score panel, recovered)
val Indigo800 = Color(0xFF21295C) // Brand deep tone; Verification-Required badge text; dark panel gradient end
val Navy700 = Color(0xFF2A3560) // Dark-surface tracks, raised dark cards, ring track on dark

val Teal600 = Color(0xFF1C7293) // Primary action, links, active nav, focus ring. 5.4:1 on white
val Teal700 = Color(0xFF065A82) // Pressed state, Recovery Pending text
val Teal50 = Color(0xFFEAF4F8) // Tinted backgrounds (Searching badge, selected chip, AI-suggested field)

val Signal500 = Color(0xFFF2A65A) // Match accent on dark: ring arcs, accent CTA on navy
val Signal600 = Color(0xFFC46F12) // Match accent on light: ring arcs, icons
val Signal800 = Color(0xFF9A5510) // Match-accent text on light. 5.7:1
val Signal50 = Color(0xFFFDF1E3) // Match Found badge background

val CanvasBg = Color(0xFFF6F8FB) // App background
val Surface = Color(0xFFFFFFFF) // Cards, sheets, inputs
val ImageBg = Color(0xFFEEF1F6) // Item photo backdrop
val BorderColor = Color(0xFFE4EAF3) // Hairlines, card borders (1 px)
val BorderStrong = Color(0xFFC9D3E0) // Input borders, disabled fills
val TextMuted = Color(0xFF5A6B85) // Secondary text. 5.4:1 on white

val Success600 = Color(0xFF1F7A52) // Verified / Recovered
val Success50 = Color(0xFFE6F5EE) // Success tint

val Warning700 = Color(0xFF8A5A00) // Low confidence / Under review text
val Warning50 = Color(0xFFFFF3D6) // Warning tint

val Error600 = Color(0xFFC23636) // Errors, destructive. 5.4:1
val Error50 = Color(0xFFFBEAEA) // Error tint
